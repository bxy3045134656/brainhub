[project]
name = "brainhub"
version = "0.1.0"
description = "统一 Web 前端 + 网盘 + MCP 网关 + 运维 agent — Brain 产品族自用主入口"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "鑫宇" }]
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "jinja2",
    "python-multipart",
    "anthropic",
    "watchdog",
    "Pillow",
    "pdf2image",
    "httpx",
    "apscheduler",
    # Windows 命名管道 listener（brain-matrix-in/agent-status 读端）需 pywin32；
    # 纯 stdlib 做不了 listener。非 Windows 平台不强装（退化到 listener 不起，写端用 open）。
    "pywin32>=305 ; sys_platform == 'win32'",
    # brainmem 本地 editable 装（共享 memory.db + 调检索/记忆 API）
    "brainmem",
]

[project.scripts]
brainhub = "brainhub.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["brainhub"]

[tool.uv.sources]
brainmem = { path = "../brainmem", editable = true }

[tool.uv]
dev-dependencies = [
    "pytest",
    "ruff",
]
